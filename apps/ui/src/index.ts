import { NotImplementedError } from '@tomo/core';

/** Stub UI entrypoint. Renders the text approval/checkout surface. */
export function startUi(): never {
  throw new NotImplementedError('ui.start');
}
